No frontmatter and no H1 here, so the page title should fall back to the filename ("appendix").

This paragraph uses a [reference-style link][spec] and a second one to a [local file][intro]. Reference links are defined out of band, which is exactly the case the href-based rewriter must still catch for the local one.

[spec]: https://commonmark.org
[intro]: intro.md
